<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BarClip.Maui.VideoLibraryPage"
             Title="Videos">

    <Grid RowDefinitions="Auto, *">

        <!-- Filter bar -->
        <HorizontalStackLayout Grid.Row="0" Padding="10" Spacing="10">
            <Button Text="All Videos" />
            <Button Text="By Session" />
        </HorizontalStackLayout>

        <!-- Video Grid -->
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Videos}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical"
                                 Span="2"
                                 HorizontalItemSpacing="4"
                                 VerticalItemSpacing="4" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="4">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnVideoTapped" />
                        </Grid.GestureRecognizers>
                        <Image Source="{Binding ThumbnailSasUrl}"
                               Aspect="AspectFill"
                               HeightRequest="200" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>

</ContentPage>